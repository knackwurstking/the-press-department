import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.knackwurstking.ThePressDepartment',
  appName: 'the-press-department',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
